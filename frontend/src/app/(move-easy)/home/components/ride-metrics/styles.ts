'use client'

import styled from 'styled-components'

export const RideMetricsContainer = styled.section`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 2rem;
  margin-top: 0.75rem;
  color: ${(props) => props.theme.colors['neutral-100']};
`
